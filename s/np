git status
git log --branches --not --remotes
git config user.name
git config user.email
git branch
#gem which cocoapods
#pod --version
#cd ios && security find-identity -v -p codesigning && xcrun agvtool what-version && cd ..
#ls ~/Library/MobileDevice/Provisioning\ Profiles/
